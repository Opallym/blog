<?php
// Déclare l’espace de noms pour le module, ici sous le framework.
namespace Framework;

/**
 * Classe représentant un module dans l'application.
 * Un module est une unité de fonctionnalités que l'on peut ajouter à une application.
 * Par exemple, un module peut gérer les utilisateurs, un autre la gestion des articles, etc.
 */
class Module
{
    /**
     * Une constante appelée DEFINITIONS.
     * Elle est actuellement définie sur null, ce qui signifie qu'aucune valeur n'est assignée.
     * Cette constante peut être utilisée pour définir des configurations ou des paramètres pour le module.
     * Par exemple, un tableau de définitions pour ce module pourrait y être stocké dans le futur.
     */
    const DEFINITIONS = null;
}
